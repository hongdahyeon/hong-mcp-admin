# Implementation Plan

---

# Plan 01.

# 프론트엔드 기본 구조 설계 및 적용

React (Vite) + TypeScript 설정을 점검하고, 프로젝트 규모에 맞는 체계적인 폴더 구조와 **라우팅(React Router)** 기반의 아키텍처를 구축합니다.

## 제안 사항 및 설계 계획

1. **TypeScript 전환**:
   - `typescript`, `tslib`, `@types/node` 등 필수 패키지 설치
   - `tsconfig.json` 및 `tsconfig.app.json`, `tsconfig.node.json` 설정 파일 생성
   - `src/vite-env.d.ts` 파일 추가
   - 기존 `.jsx` 파일을 `.tsx`로 변환 (`App.tsx`, `main.tsx`)
2. **폴더 구조 자동화**: `src/` 하위에 다음과 같은 폴더를 생성합니다:
   - `api/`: API 호출 로직 (Axios 인스턴스 등)
   - `components/`: 재사용 가능한 UI 컴포넌트 (`common/`, `layout/`)
   - `hooks/`: 커스텀 Hook
   - `pages/`: 라우팅되는 페이지 컴포넌트
   - `routes/`: 라우터 설정 및 경로 매핑 (URL ↔ Component)
   - `store/`: 전역 상태 관리 (Zustand/Redux 등)
   - `types/`: 공통 TypeScript 인터페이스 및 타입 정의
   - `utils/`: 유틸리티 함수 및 상수

## 검증 계획

### 자동화 테스트

- `npm run build`를 통한 TypeScript 타입 체크 및 빌드 성공 여 확인

### 수동 검증

- `npm run dev`를 통한 로컬 개발 환경에서의 정상 실행 확인

---

# Plan 02.

# Tailwind CSS 및 의존성 설치

**Tailwind CSS**와 **React Router**, **Lucide React**를 프로젝트에 도입하여 스타일링 및 라우팅 기반을 마련합니다.

## 제안 사항 및 설계 계획

1. **Tailwind CSS 및 의존성 설치**:
   - `tailwindcss`, `postcss`, `autoprefixer` 설치 및 초기화
   - `react-router-dom`, `lucide-react` (아이콘용) 설치

2. **기본 레이아웃 및 라우팅 구현**:
   - `src/components/layout/`: `Header`, `Footer`, `MainLayout` 컴포넌트 생성
   - `src/pages/home/`: 기본 홈 페이지 생성
   - `src/routes/`: `AppRouter.tsx`를 통한 경로 매핑

3. **컴포넌트 상세 설계 (Header)**:
   - 상단 메뉴 및 호버 시 드롭다운 바 구현
   - 우측 사용자 아이콘 및 드롭다운 메뉴 (Login/Logout 분기 처리)

## 검증 계획

### 자동화 테스트

- `npm run build`를 통한 빌드 성공 여 확인

### 수동 검증

- `npm run dev`를 통한 개발 서버 실행
- 헤더 컴포넌트의 렌더링 및 드롭다운 메뉴 호버 동작 확인
- 라우팅을 통한 페이지 이동 테스트

---

# Plan 03.

# 기본 레이아웃 및 라우팅 구현

**Tailwind CSS**와 **React Router**를 도입하여 표준적인 어드민 플랫폼 레이아웃(Header, Body, Footer)을 구축합니다.

## 제안 사항 및 설계 계획

1. **기본 레이아웃 및 라우팅 구현**:
   - `src/components/layout/`: `Header`, `Footer`, `MainLayout` 컴포넌트 생성
   - `src/pages/home/`: 기본 홈 페이지 생성
   - `src/routes/`: `AppRouter.tsx`를 통한 경로 매핑

2. **컴포넌트 상세 설계 (Header)**:
   - 상단 메뉴 및 호버 시 드롭다운 바 구현
   - 우측 사용자 아이콘 및 드롭다운 메뉴 (Login/Logout 분기 처리)

## 검증 계획

### 자동화 테스트

- `npm run build`를 통한 빌드 성공 여 확인

### 수동 검증

- `npm run dev`를 통한 개발 서버 실행
- 헤더 컴포넌트의 렌더링 및 드롭다운 메뉴 호버 동작 확인
- 라우팅을 통한 페이지 이동 테스트

---

# Plan 04.

# 공방 관리 서비스 전환 및 CraftDay 브랜딩

기존 어드민 대시보드 구조를 '원데이 클래스 공방 플랫폼' 테마인 **CraftDay**로 전환하고, 사용자 중심의 칸반 보드 레이아웃을 적용합니다.

## 제안 사항 및 설계 계획

1. **워크숍 칸반 보드**:
   - 서울, 경기, 부산, 제주 등 지역별 컬럼 생성 및 수평 스크롤 레이아웃 적용
   - 고해상도 이미지, 별점, 가격, 작가 정보가 포함된 프리미엄 카드 디자인
2. **브랜딩 일원화**:
   - Pretendard 폰트 전역 적용 및 Violet (violet-600) 메인 컬러 시스템 구축
   - Header, Footer에 'CraftDay' 로고 및 슬로건 반영
3. **사용자 경험(UX) 개선**:
   - 로그인 전/후 헤더 메뉴 동적 전환 (드롭다운 통합)
   - 로그인 페이지 내 '둘러보기' 기능 추가로 비회원 접근성 향상

## 검증 계획

- 모든 브랜딩 텍스트의 'CraftDay' 일치 여부 확인
- 지역별 워크숍 필터링 및 렌더링 정상 동작 확인

---

# Plan 05.

# 인증(Auth) 인프라 및 보안 강화

JWT 기반의 인증 시스템을 구축하고, API 요청 시 보안성을 확보하며 예외적인 페이지 접근에 대한 처리를 구현합니다.

## 제안 사항 및 설계 계획

1. **Axios 인터셉터**:
   - 모든 요청 헤더에 `Authorization: Bearer <token>` 자동 주입
   - 401 Unauthorized 발생 시 자동 세션 만료 알림 및 로그인 페이지 리다이렉트
2. **인증 서비스 (`authService`)**:
   - `localStorage`를 활용한 토큰 및 유저 정보 관리 로직 캡슐화
3. **에러 핸들링**:
   - 정의되지 않은 경로 접근 시 전용 404 (`NotFound`) 페이지 노출

## 검증 계획

- 로그아웃 후 보호된 API 요청 시 401 처리 여부 확인
- 잘못된 URL 입력 시 404 페이지 정상 노출 여부 확인

---

# Plan 06.

# 회원가입(Signup) 기능 구현

사용자가 직접 계정을 생성할 수 있는 회원가입 플로우를 구현하며, 실시간 유효성 검사와 API 연동을 처리합니다.

## 제안 사항 및 설계 계획

1. **API 연동 (`auth.ts`)**:
   - `/api/auth-user/check-email`, `/api/auth-user/check-username` 중복 확인 API 연동
   - `/api/auth-user/roles` API를 통한 가입 가능 역할군 동적 로드
   - `/api/auth-user/signup` API를 통한 최종 가입 데이터 송신
2. **회원가입 UI (`Signup`)**:
   - 이메일, 사용자명, 비밀번호, 비밀번호 확인, 역할 선택 필드 구성
   - 각 필드별 실시간 유효성 메시지 및 중복 체크 결과 피드백
3. **네비게이션**:
   - 가입 성공 시 로그인 페이지로 자동 이동 및 환영 메시지 노출

## 검증 계획

- 중복된 이메일/ID로 가입 시도 시 에러 메시지 노출 확인
- 비밀번호 불일치 시 가입 버튼 비활성화 여부 확인
- 가입 성공 후 실제 로그인 가능 여부 테스트

---

# Plan 07.

# 폰트 및 테마 최적화 (Tailwind CSS v4)

프로젝트의 시각적 완성도를 높이기 위해 **Pretendard Variable** 폰트를 적용하고, Tailwind CSS v4의 `@theme` 기능을 활용하여 테마 설정을 최적화합니다.

## 제안 사항 및 설계 계획

1. **폰트 시스템 업데이트**:
   - `index.css`에서 `:root` 설정 대신 Tailwind CSS v4의 `@theme` 블록 사용
   - `Pretendard Variable`을 최우선 순위로 지정하여 현대적인 타이포그래피 구현
   - 시스템 폰트 폴백(fallback) 구성을 통해 다양한 환경에서의 안정성 확보

2. **디자인 시스템 연동**:
   - `--font-sans` 변수를 통해 프로젝트 전반에 일관된 폰트 스타일 적용

## 검증 계획

### 수동 검증

- 브라우저 개발자 도구를 통해 `body`에 `font-family: var(--font-sans)`가 정상적으로 적용되는지 확인
- 한글 및 영문 텍스트의 가독성 및 렌더링 상태 점검

---

# Plan 08.

# 전역 예외 처리 최적화 (ErrorResponse / HongErrorCode 연동)

백엔드에서 발생하는 `HongException`이 `HongErrorCode`를 통해 구조화된 `ErrorResponse`를 반환하도록 변경됨에 따라, 프론트엔드 Axios 인터셉터를 이에 맞춰 최적화합니다.

## 제안 사항 및 설계 계획

1. **상세 에러 구조 대응**:
   - `ErrorResponse` 구조(`code`, `message`, `status`)에 맞춰 응답 데이터를 파싱합니다.
   - 단순히 메시지만 보여주는 것이 아니라, 필요 시 에러 코드(`code`)를 활용한 분기 처리가 가능하도록 기반을 마련합니다.

2. **Axios Response Interceptor 고도화**:
   - `data.message`가 존재할 경우 최우선적으로 사용자에게 노출합니다.
   - **401 에러 세분화**: 단순히 `status === 401`인 경우 세션 만료로 처리하지 않고, `code`가 `A001`(Unauthorized), `A002`(Invalid Token) 등 인증 시스템 관련 에러인 경우에만 로그아웃 처리를 수행합니다. `U002`(Password Not Match)와 같은 비즈니스 예외는 메시지만 노출합니다.
   - 에러 코드와 함께 로그를 남겨 디버깅 편의성을 향상시킴으로써 디버깅 편의성을 향상시킵니다.

## 검증 계획

### 수동 검증

- 존재하지 않는 계정(`USER_NOT_FOUND`) 또는 비밀번호 불일치(`PASSWORD_NOT_MATCH`) 시 백엔드에서 내려주는 각기 다른 `message`가 정확히 노출되는지 확인합니다.
- 중복된 이메일/이름 가입 시도 시 `BAD_REQUEST(400)` 상태와 함께 구체적인 에러 메시지가 표시되는지 확인합니다.

---

# Plan 09.

# CI/CD 파이프라인 구축 (Jenkinsfile)

Java 25 (Spring Boot 4) 백엔드와 React 프론트엔드 프로젝트의 자동 빌드 및 머지 시스템을 구축합니다.

## 제안 사항 및 설계 계획

1. **Gatekeeper 전략 적용**:
   - 피처 브랜치에서 `work` 브랜치로의 자동 머지 및 푸시 로직 구현.
   - `work` 브랜치에서 피처 브랜치로의 동기화(Sync) 상황 자동 감지 및 알림.

2. **환경 구성**:
   - Java 25 (JDK 25) 및 Node.js 환경에서의 빌드 지원.
   - 텔레그램 봇을 통한 실시간 CI/CD 상태 알림 연동.

## 검증 계획

### 자동화 테스트

- Jenkins 파이프라인 실행을 통한 빌드, 테스트, 머지 전 과정 성공 여 확인.
- 텔레그램 알림 메시지 도달 확인.---

---

# Plan 10.

# 레이아웃 고도화 및 테마 시스템 구축

사용자 경험 향상을 위해 전역 다크 모드 기능을 도입하고, 넓은 화면에서도 안정적인 레이아웃을 제공하며, 로그인 편의성을 위해 아이디 기억 기능을 구현합니다.

## 제안 사항 및 설계 계획

1. **전역 테마 시스템 (`ThemeContext`)**:
   - `React Context API`를 활용하여 `ThemeProvider` 구현.
   - `localStorage`와 연동하여 테마 설정 유지 및 브라우저 기본 설정(Media Query) 대응.
   - Tailwind CSS v4 `@variant dark` 설정을 통한 클래스 기반 다크 모드 지원.

2. **레이아웃 및 반응형 최적화**:
   - 메인 컨텐츠 영역에 `max-w-[1440px]` 및 `mx-auto`를 적용하여 광활한 화면에서도 가독성 확보.
   - 모든 주요 컴포넌트(Header, Footer, Home, Login, Signup)에 다크 모드 스타일(`dark:`) 적용.

3. **로그인 편의 기능**:
   - `localStorage` 키 `HONG_CRAFT_DAY`를 활용한 아이디Persistence 구현.
   - 체크박스 상태에 따른 아이디 저장/삭제 로직 및 자동 완성 기능.

## 검증 계획

### 수동 검증

- 헤더의 테마 토글 버튼을 통해 다크/라이트 모드가 즉각적으로 모든 페이지에 반영되는지 확인.
- 로그인 페이지에서 '아이디 기억하기' 체크 후 로그인 시, 재방문 시 아이디가 자동 입력되는지 확인.
- 2560px 이상의 고해상도 모니터에서 레이아웃이 좌우로 과하게 퍼지지 않고 중앙에 안정적으로 배치되는지 확인.

---

# Plan 11.

# 인증 정보 관리 최적화 및 UI 가독성 개선

사용자 인증 데이터를 효율적으로 관리하기 위해 저장 구조를 통합하고, 서비스 접근성을 높이기 위해 푸터 링크 추가 및 헤더 UI를 개선합니다.

## 제안 사항 및 설계 계획

1. **상세 에러 구조 대응**:
   - `accessToken`, `refreshToken`, `username`, `role`을 `AUTH_DATA`라는 단일 JSON 객체로 통합하여 관리.
   - `authService` 및 관련 컴포넌트(`Header`, `Footer`) 소스 코드 수정.

2. **UI/UX 개선**:
   - 푸터에 "나도 공방 주인! 공방 등록하기" 링크 추가 (비회원: 로그인, 회원: 공방 등록 페이지 유도).
   - 헤더 네비게이션 메뉴 및 드롭다운 메뉴 폰트 크기 확대를 통한 가독성 강화.
   - `ROLE_ADMIN` 전용 시스템 관리 메뉴(사용자/공방/이력/결제 관리) 추가.
   - 어드민 하위 페이지(user, workplace, access, payment) 프로토타입 구현 및 헤더 링크 연동.

## 검증 계획

### 수동 검증

- `localStorage`에 `AUTH_DATA` 키 하나만 정상적으로 생성/삭제되는지 확인.
- 푸터 링크 클릭 시 로그인 상태에 따른 정확한 페이지 리다이렉션 확인.
- 브라우저에서 헤더 메뉴 글자 크기가 의도한 대로 확대되었는지 확인.

---

# Plan 12.

# 공통 페이징 처리 및 실데이터 연동

어드민의 모든 목록성 데이터를 일관성 있게 관리하기 위해 페이징(Pagination) 요청/응답 구조를 공통 타입으로 정의하고, 실제 백엔드 API와 연동합니다.

## 제안 사항 및 설계 계획

1. **공통 페이징 타입 정의 (`types/common.ts`)**:
   - `PageRequestDto`: `page`, `size`, `search` 등 요청 파라미터 정의.
   - `PageResponseDto<T>`: `content`, `totalPages`, `totalElements` 등 백엔드 응답 구조와 매칭.

2. **실데이터 연동 (Access Log)**:
   - `/api/admin/user-access/page` 엔드포인트 연동.
   - `useEffect`와 `useState`를 활용한 상태 기반 데이터 패칭 로직 구현.
   - 로딩 상태 처리 및 검색어 통합 필터링.

3. **인증 토큰 주입 방식 수정**:
   - `localStorage`의 `AUTH_DATA` JSON 객체에서 `accessToken`을 정확히 추출하도록 Axios 인터셉터 수정.

## 검증 계획

### 수동 검증

- 접속 이력 페이지 진입 시 로딩 표시 후 실제 DB 데이터가 노출되는지 확인.
- 하단 페이지네이션 클릭 시 요청 파라미터(`page`)가 변경되어 새로운 데이터를 불러오는지 확인.
- 검색어 입력 시 `search` 파라미터와 함께 필터링된 결과가 반환되는지 확인.

---

# Plan 13.

# 토큰 재발급 (Token Reissue) 플로우 구현

Access Token이 만료되었을 때 (`A005` 에러), 사용자가 로그아웃되지 않고 Refresh Token을 통해 새로운 Access Token을 발급받아 기존 요청을 자동으로 재시도하는 'Silent Refresh' 로직을 심플하게 구현합니다.

## 제안 사항 및 설계 계획

1. **`authService.reissue` 메서드 활용**:
   - `/api/auth/reissue` 엔드포인트에 `refreshToken`을 담아 요청.
   - `A005` 에러 발생 시에만 동작하도록 단순화합니다.

2. **Axios Response Interceptor 단순화**:
   - 응답 데이터의 `code` 값이 `A005`인 경우 즉시 `reissue`를 시도합니다.
   - 재발급 시도 중 다시 `A005`가 발생하거나 실패하면 즉시 세션 만료 알림을 띄우고 로그인 페이지로 이동합니다.
   - 그 외의 모든 에러는 `ErrorResponse`의 `message`를 그대로 활용하여 경고창을 띄웁니다.

3. **중복 방지 로직 유지**:
   - 최소한의 플래그(`isRefreshing`)를 활용하여 찰나의 순간에 중복 재발급 요청이 나가는 것을 방지합니다.

## 검증 계획

### 수동 검증

- Access Token을 고의로 만료시킨 후 페이지를 새로고침하거나 API를 호출했을 때, 자동으로 토큰이 갱신되고 데이터가 정상적으로 불러와지는지 확인.
- `localStorage`의 `AUTH_DATA` 내 토큰 값이 변경되는지 확인.
- Refresh Token까지 만료되었을 때 정상적으로 로그인 페이지로 리다이렉트되는지 확인.

---

# Plan 14.

# 장바구니 및 관심(하트) 기능 구현

글로벌 상태(`CartContext`)를 통해 장바구니와 관심 목록을 관리하고, 헤더에서 실시간 카운트 배지와 미리보기 드롭다운을 제공합니다.

## 제안 사항 및 설계 계획

1. **전역 상태 관리 (`CartContext`)**:
   - `cartItems` 및 `favoriteItems` 배열을 관리하는 Context 생성.
   - 로컬 스토리지 연동 없이 메모리 상에서 관리 (필요 시 추후 연동 가능).
   - `addToCart`, `toggleFavorite` 등 핵심 액션 제공.

2. **헤더 인터페이스 고도화**:
   - 헤더 우측 상단에 `Heart` 및 `ShoppingCart` 아이콘 배치.
   - 각 아이콘 위에 현재 담긴 수량을 표시하는 Floating Badge 적용.
   - 클릭 시 최근 담은 항목(최대 5개)을 보여주는 Floating Dropdown 구현.
   - 드롭다운 최하단에 '더보기' 버튼을 통해 전체 페이지로 유도.

3. **워크숍 카드 연동**:
   - 홈 페이지의 워크숍 카드 내 하트 및 장바구니 버튼 클릭 시 전역 상태 업데이트.
   - 상태 변화 시 헤더 배지 숫자가 즉각적으로 애니메이션과 함께 변경되도록 처리.

4. **전체 보기 페이지 및 라우팅**:
   - `/cart`, `/favorites` 경로 추가.
   - 전체 목록을 시각적으로 확인할 수 있는 독립된 페이지 구현.

## 검증 계획

### 수동 검증

- 홈 화면에서 여러 개의 공방을 장바구니/관심 목록에 추가하여 헤더 숫자가 올라가는지 확인.
- 헤더 아이콘 클릭 시 드롭다운이 정상적으로 열리고 리스트가 노출되는지 확인.
- '더보기' 클릭 시 전체 목록 페이지로 정상 이동하는지 확인.

---

# Plan 30.

# 이용약관 및 개인정보처리방침 페이지 구현

사용자가 서비스 이용 규칙과 개인정보 보호 방침을 명확히 확인할 수 있도록 텍스트 기반의 전용 페이지를 구축하고, 푸터(Footer)를 통해 접근할 수 있게 합니다.

## 제안 사항 및 설계 계획

1. **Routing 설정**:
   - `frontend/src/routes/AppRouter.tsx`: `/terms` (이용약관), `/privacy` (개인정보처리방침) 경로 추가.
   
2. **페이지 컴포넌트 구현**:
   - `frontend/src/pages/legal/Terms.tsx` [NEW]: 이용약관 텍스트 컨텐츠를 담은 페이지.
   - `frontend/src/pages/legal/Privacy.tsx` [NEW]: 개인정보처리방침 텍스트 컨텐츠를 담은 페이지.
   - 가독성을 위해 적절한 레이아웃(최대 너비 제한, 여백)과 타이포그래피 적용.

3. **푸터 연동**:
   - `frontend/src/components/layout/Footer.tsx`: 기존의 단순 텍스트(`span`)를 `Link` 컴포넌트나 `navigate`를 활용한 이동 링크로 변환.

## 검증 계획

### 수동 검증
- 푸터 하단의 '이용약관' 및 '개인정보처리방침' 클릭 시 각각 올바른 페이지로 이동하는지 확인.
- 텍스트 위주의 페이지에서 모바일 가동성(가독성) 및 레이아웃이 정상인지 확인.
- 다크 모드 적용 시 텍스트 컬러가 배경색과 잘 조화되는지 확인.

---

# Plan 31.

# 백엔드 API 응답 구조 변경 대응 (BaseResponse 도입)

백엔드 API의 응답 구조가 `BaseResponse<T>` (message, data 포함) 형식으로 변경됨에 따라, 프론트엔드의 공통 타입과 실제 API 호출부(`AccessLog.tsx`)를 수정하여 연동을 정상화합니다.

## 제안 사항 및 설계 계획

1. **공통 타입 정의**:
   - `frontend/src/types/common.ts`: 백엔드의 `BaseResponse<T>`와 대응하는 인터페이스 추가.
     ```typescript
     export interface BaseResponse<T> {
         message: string;
         data: T;
     }
     ```

2. **API 호출 로직 수정**:
   - `frontend/src/pages/admin/access/index.tsx`:
     - `api.get`의 제네릭 타입을 `BaseResponse<PageResponseDto<UserAccessLogList>>`로 변경.
     - `response.data.data`를 통해 실제 데이터(`PageResponseDto`)에 접근하도록 수정.

3. **영향 범위 확인**:
   - 현재 실데이터를 연동 중인 다른 관리자 페이지 및 서비스가 있는지 전수 조사하고, 필요 시 동일한 구조로 수정 적용.

## 검증 계획

### 수동 검증
- 접속 이력 관리 페이지 진입 시 데이터가 정상적으로 로드되는지 확인.
- 브라우저 개발자 도구의 Network 탭을 통해 응답 데이터가 `BaseResponse` 구조인지, 프론트엔드에서 이를 정확히 파싱하는지 확인.

# 공방 리스트 페이지 및 다중 필터 검색 시스템

상세 검색 필터링이 가능한 공방 리스트 페이지를 구축하여 사용자가 원하는 클래스를 쉽게 찾을 수 있도록 합니다.

## 제안 사항 및 설계 계획

1. **Home Page 내비게이션 추가**:
   - 각 지역(Region) 섹션 제목 우측에 `자세히 보기 >` 링크 추가.
   - 클릭 시 `/workshops?region={region}`으로 이동하여 해당 지역이 자동 필터링되도록 처리.

2. **Workshop List Page (`/workshops`) 구현**:
   - **통합 검색바**: 공방명(텍스트 검색) 입력 필드.
   - **다중 필터링 폼**:
     - 지역(Seoul, Gyeonggi, etc.) 선택.
     - 카테고리(Craft, Pottery, etc.) 선택.
     - 가격 범위 슬라이더 또는 선택창.
     - 최소 별점 및 후기 개수 필터.
   - **카드 리스트 레이아웃**: 검색 결과에 맞는 공방들을 그리드 또는 리스트 형태로 출력.

3. **필터 로직 및 상태 관리**:
   - `URLSearchParams`를 활용하여 필터 상태를 URL에 동기화 (공유 가능성 및 뒤로가기 지원).
   - 필터 변경 시 `useEffect`를 통해 실시간으로 데이터 필터링 처리.

4. **컴포넌트 재사용**:
   - 홈 화면에서 사용한 공방 카드를 리스트 페이지에서도 재사용하여 일관성 유지.
   - `CartContext` 연동을 통해 리스트에서도 즉시 장바구니/찜 가능.

## 검증 계획

### 수동 검증

- 홈 화면의 '자세히 보기'를 눌렀을 때 해당 카테고리/지역이 필터링된 리스트 페이지로 이동하는지 확인.
- 검색 폼의 각 필드(이름, 지역, 가격 등)를 조작할 때 리스트가 즉각 반영되는지 확인.
- 필터 조건이 복합적일 때(예: 서울 + 공예 + 4.5점 이상) 정확한 결과를 도출하는지 확인.

---

# Plan 16.

# 공방 상세 정보 페이지 (`WorkshopDetail`)

사용자가 특정 공방의 상세 정보(커리큘럼, 강사 정보, 리뷰 등)를 확인하고 즉시 예약/구매 결정을 내릴 수 있도록 몰입감 있는 상세 페이지를 구축합니다.

## 제안 사항 및 설계 계획

1. **Routing 및 내비게이션**:
   - `frontend/src/routes/AppRouter.tsx`: `/workshops/:id` 동적 경로 추가.
   - 홈, 리스트, 장바구니 등 모든 워크숍 카드 클릭 시 상세 페이지로 이동하도록 UI 업데이트.

2. **Workshop Detail Page (`/workshops/:id`) 구현**:
   - **대표 섹션**: 공방 대표 이미지, 제목, 평점, 위치 등 핵심 요약 정보.
   - **상세 탭/섹션**:
     - 소개: 클래스 소개 및 상세 설명.
     - 커리큘럼: 회차별 수업 내용.
     - 강사: 강사 프로필 및 전문 분야.
     - 리뷰: 사용자들의 리얼 리뷰 목록.
   - **하단 고정바 (Sticky Bar)**: 가격 표시와 함께 '장바구니 담기' 및 '관심 등록' 버튼을 하단에 고정하여 구매 전환 유도.

3. **데이터 연동**:
   - `MOCK_WORKSHOPS`에서 ID를 기반으로 해당 공방 정보 추출.
   - `CartContext` 연동을 통해 상세 페이지에서도 즉시 상태 반영.

## 검증 계획

### 수동 검증

- 서로 다른 ID의 공방 카드를 클릭했을 때 각각 올바른 상세 정보가 표시되는지 확인.
- 상세 페이지 내 찜하기(하트) 버튼 클릭 시 헤더의 위시리스트 숫자가 동기화되는지 확인.
- 모바일 가로폭에서 하단 고정 예약바가 가독성 있게 표시되는지 확인.

---

# Plan 17.

# 공방 등록 및 운영 관리 시스템 (`WorkshopManagement`)

공방 운영자가 자신의 클래스를 직접 등록하고 관리할 수 있는 시스템을 구축합니다.

## 제안 사항 및 설계 계획

1. **Routing 설정**:
   - `frontend/src/routes/AppRouter.tsx`:
     - `/workshops/manage`: 내 공방 목록 및 관리 대시보드.
     - `/workshops/manage/new`: 신규 공방 등록 폼.

2. **기능 구현**:
   - **공방 등록 (`/workshops/manage/new`)**:
     - 이미지 업로드(Mock), 제목, 카테고리, 지역, 가격 정보 입력.
     - 상세 설명 및 커리큘럼(리스트 형태) 입력 폼 제공.
     - `react-hook-form` 등을 고려한 유효성 검사.
   - **공방 관리 (`/workshops/manage`)**:
     - 내가 등록한 공방들을 리스트 형태로 표시.
     - 각 항목별 수정/삭제 버튼 배치 (UI 우선).
   - **Header 내비게이션**:
     - 사용자 프로필 드롭다운 또는 메뉴에 '내 공방 관리' 링크 추가.

3. **데이터 처리**:
   - 등록된 데이터는 로컬 상태(`useState`) 또는 `localStorage`를 활용하여 새로고침 시에도 유지되도록 구현 (Mock 서버 연동 전 단계).

## 검증 계획

### 수동 검증

- 모든 필수 입력 항목을 채우고 '등록' 버튼을 눌렀을 때 관리 페이지 리스트에 추가되는지 확인.
- 등록된 공방의 '수정' 버튼 클릭 시 정보를 수정할 수 있는 폼이 나타나는지 확인.
- 헤더의 메뉴를 통해 관리 페이지로 정상 접근 가능한지 확인.

---

# Plan 18.

# 워크숍 예약 시스템 구현 (`ReservationModal`)

공방 상세 페이지에서 즉시 예약을 진행할 수 있는 프리미엄 모달 팝업 시스템을 구축합니다.

## 제안 사항 및 설계 계획

1. **컴포넌트 구조**:
   - `frontend/src/pages/workshops/components/ReservationModal.tsx` [NEW]:
     - 날짜/시간/인원 선택을 포함한 독립적인 모달 컴포넌트.
   - `WorkshopDetail.tsx` [MODIFY]:
     - '예약하기' 버튼 클릭 시 모달 노출 로직 추가.

2. **주요 UI/UX 요소**:
   - **Step-by-Step UI**: 날짜 -> 시간 -> 인원 순으로 자연스러운 흐름 유도.
   - **캘린더 뷰**: `react-calendar` 또는 라이브러리 없이 직접 구현하여 직관적인 날짜 선택 제공.
   - **타임 슬롯**: 오전/오후 등 가능한 시간대를 칩(Chip) 형태로 나열.
   - **참가자 카운터**: `+`, `-` 버튼을 통한 인원 조정 및 최대 인원 제한 반영.

3. **데이터 처리**:
   - 예약 데이터(날짜, 시간, 인원)는 로컬 상태로 관리.
   - '결제/예약 완료' 클릭 시 성공 알림(Toast/Alert) 후 모달 닫기 (Mock 연동).

## 검증 계획

### 수동 검증

- 모달이 열렸을 때 배경 스크롤이 차단되는지 확인.
- 날짜를 선택하지 않으면 시간 선택이 비활성화되는지 확인.
- 인원 수 변경에 따른 최종 결제 금액 합계가 올바르게 계산되는지 확인.
- '예약 완료' 시 사용자에게 성공 피드백이 명확히 전달되는지 확인.

---

## 19. 공방 예약 관리 시스템 (관리자/작가용)

공방을 등록한 작가가 자신의 클래스에 들어온 예약 내역을 확인하고 승인(확정) 또는 반려(거절) 처리하는 기능을 구현합니다.

### 주요 변경 사항

#### [NEW] [Reservations.tsx]

- 특정 공방(ID 기반)의 예약 신청 목록을 표시하는 관리 페이지.
- 사용자명, 예약 일시, 인원수, 현재 상태(대기/확정/취소)를 테이블 또는 리스트로 노출.

#### [MODIFY] [Manage.tsx]

- 각 공방 카드에 "예약 내역" 버튼 추가.
- 클릭 시 해당 공방의 예약 관리 페이지로 이동.

#### [NEW] [RejectModal.tsx]

- 예약 반려 시 거절 사유를 입력받기 위한 팝업 모달.

### 데이터 모델 (Mock)

- `Reservation`: `{ id, workshopId, userName, userEmail, date, time, guests, status, rejectReason? }`

### 검증 계획

- 관리자 페이지에서 예약 목록이 정상적으로 표시되는지 확인.
- 승인 버튼 클릭 시 상태가 '확정'으로 변경되는지 확인.
- 반려 버튼 클릭 시 모달이 뜨고, 사유 입력 후 상태가 '실패(반려)'로 변경되는지 확인.

---

# Plan 20.

# 내 예약 관리 페이지 (사용자용)

사용자가 자신이 예약한 모든 공방 클래스 목록을 확인하고, 예약 상태 및 상세 내역을 관리할 수 있는 마이페이지 기능을 구현합니다.

## 제안 사항 및 설계 계획

1. **Routing 설정**:
   - `frontend/src/routes/AppRouter.tsx`: `/my/reservations` 경로 추가.

2. **기능 구현**:
   - **내 예약 리스트 (`/my/reservations`)**:
     - 내가 예약한 모든 공방 목록을 카드 또는 리스트 형태로 표시.
     - 각 항목별 공방 정보(이미지, 제목, 카테고리) 및 예약 정보(날짜, 시간, 인원, 금액, 상태) 노출.
   - **상태 표시**:
     - '대기 중', '확정됨', '반려됨' 상태를 시각적으로 구분하여 표시.
     - 반려 시 거절 사유 확인 버튼/툴팁 제공.
   - **Header & User Menu 연동**:
     - 헤더 '예약 내역' 링크 및 사용자 메뉴 드롭다운 내 '예약 내역 확인' 버튼을 `/my/reservations`로 연결.

3. **데이터 처리**:
   - `localStorage` 또는 `MOCK_RESERVATIONS` 상수를 활용하여 예약 데이터를 통합 관리.
   - 필요 시 `reservationService` (Mock)를 추가하여 데이터 페칭 로직 추상화.

## 검증 계획

### 수동 검증

- 헤더 및 사용자 메뉴를 통해 예약 관리 페이지로 정상 이동하는지 확인.
- 예약 목록이 최신순으로 정렬되어 필터링(상태별)되는지 확인.
- 각 상태(대기/확정/반려)에 따른 UI 컬러 및 레이블이 정확히 반영되는지 확인.

---

# Plan 21.

# 커뮤니티 > 찐 후기 자랑 페이지 구현

공방 체험을 완료한 사용자들이 자신의 결과물을 공유하고 소통할 수 있는 커뮤니티 공간을 구축합니다.

## 제안 사항 및 설계 계획

1. **Routing 설정**:
   - `frontend/src/routes/AppRouter.tsx`: `/community/reviews` 경로 추가.

2. **Review Pride Page (`/community/reviews`) 구현**:
   - **피드 레이아웃**: 인스타그램 스타일의 카드 그리드(Masonry 또는 정방형) 레이아웃 적용.
   - **베스트 후기**: 상단에 이달의 베스트 후기 3건을 강조하여 표시 (슬라이더 또는 상단 고정).
   - **카테고리 필터**: 공예, 베이킹, 드로잉 등 카테고리별 후기를 모아볼 수 있는 칩(Chip) 필터링.
   - **상세 보기 (Modal)**: 카드 클릭 시 큰 이미지와 함께 전체 내용, 별점, 댓글 목록을 보여주는 모달 레이아웃.

3. **후기 작성 및 연동**:
   - 내 예약 내역(`CONFIRMED` 상태)에서 '후기 남기기' 버튼을 통해 작성 폼 연동.
   - `MOCK_REVIEWS` 데이터를 통해 초기 피드 콘텐츠 구성.

## 검증 계획

### 수동 검증

- 커뮤니티 메뉴를 통해 후기 자랑 페이지로 정상 이동하는지 확인.
- 후기 카드 클릭 시 상세 모달이 뜨고 이미지가 선명하게 노출되는지 확인.
- 카테고리 필터 클릭 시 해당 카테고리의 후기만 필터링되는지 확인.
- 모바일 환경에서 1~2열 그리드 레이아웃이 깔끔하게 유지되는지 확인.

---

# Plan 22.

# 커뮤니티 > 후기 작성 기능 구현

찐 후기 자랑하기 기능을 완성하기 위해 사용자가 방문한 공방에 대한 후기를 직접 작성할 수 있는 폼과 프로세스를 구현합니다.

## 제안 사항 및 설계 계획

1. **Routing 및 내비게이션**:
   - `frontend/src/routes/AppRouter.tsx`: `/community/reviews/write` 경로 추가 (또는 모달로 구현).
   - 커뮤니티 페이지 내에 '후기 작성하기' 플로팅 버튼 또는 메뉴 상단 버튼 추가.

2. **공방 선택 플로우**:
   - 사용자가 예약 및 방문을 완료한 공방(상태가 `CONFIRMED` 또는 방문 완료인 경우)의 목록을 먼저 팝업/셀렉트로 보여주고, 리뷰할 클래스를 선택하게 함.

3. **작성 폼 컴포넌트 구현**:
   - **별점**: 1~5점 별점 컴포넌트(반개 단위 또는 1개 단위) (Lucide Icon 활용).
   - **사진 첨부**: 로컬 파일 시스템에서 이미지 다중 업로드 UI, 미리보기 제공 (Mock 처리).
   - **내용 작성**: 일반 텍스트 에어리어 혹은 심플한 에디터(Editor) 형태 제공. (간단하게 텍스트 박스로 하되, 툴바 UI로 꾸미거나 실제 에디터 라이브러리 사용 가능).
   - **데이터 처리**: 폼 내 상태 관리 및 Validation 로직 (별점 필수, 사진 첨부, 후기 길이 최소/최대 등).

4. **완료 및 피드 연동**:
   - 작성 완료 버튼 클릭 시 `MOCK_REVIEWS`에 데이터 push (또는 서버 API Mock 연동).
   - 작성 후 후기 커뮤니티 리스트로 리다이렉션하여 방금 쓴 글을 보여줌.

## 검증 계획

### 수동 검증

- '후기 작성하기' 진입 시 예약 내역 모달창이 정상적으로 열리고, 클래스를 선택할 수 있는지 확인.
- 사진 첨부 시 썸네일 미리보기가 올바르게 출력되는지 확인.
- 별점과 내용을 입력한 후 작성 완료 시 정상 처리 후 커뮤니티 피드로 넘어가는지 확인.

---

# Plan 23.

# 커뮤니티 > 공방 소식 페이지 구현

커뮤니티 메뉴의 두 번째 하위 항목인 '공방 소식' 페이지를 구현하여 각 공방의 이벤트, 신규 클래스 오픈, 공지사항 등을 매거진 형태로 제공합니다.

## 제안 사항 및 설계 계획

1. **Routing 및 내비게이션**:
   - `frontend/src/routes/AppRouter.tsx`: `/community/news` 경로 추가.
   - `frontend/src/components/layout/Header.tsx`: '공방 소식' 서브메뉴의 링크를 `/community/news`로 변경.

2. **Mock 데이터 구성 (`constants/news.ts`)**:
   - `id`, `title`, `summary`, `content`, `category`(이벤트, 신규 오픈, 공지 등), `author`(공방 이름), `thumbnail`, `date`, `viewCount` 등 정의.

3. **공방 소식 리스트 페이지 (`WorkshopNews.tsx`) 구현**:
   - 블로그/매거진 스타일 레이아웃. 상단에는 중요한 이벤트 배너를 띄우고, 하단에는 소식들이 리스트나 그리드 형태로 노출.
   - 카테고리 필터(예: 전체, 이벤트, 신규 오픈, 클래스 소식).

4. **상세 보기 (Modal or Expandable)**:
   - 각 소식 카드를 클릭했을 때 해당 내용을 자세히 볼 수 있는 상세 뷰 모달(Modal) 구현 (후기 자랑하기의 상세 팝업과 통일성 유지).

## 검증 계획

### 수동 검증
- 헤더 메뉴를 통해 정상적으로 페이지( `/community/news` )에 진입하는지 확인.
- 정의된 카테고리별로 리스트가 잘 필터링되어 노출되는지 확인.
- 소식 카드 클릭 시 모달창이 정상 작동하고 에러 없이 상세 내용을 보여주는지 확인.

---

# Plan 24.

# 커뮤니티 > 작가님 인터뷰 페이지 구현

커뮤니티의 세 번째 메뉴인 '작가님 인터뷰'를 구현하여, CraftDay 소속 공방 작가들의 진솔한 창작 스토리와 Q&A를 감성적인 에디토리얼(매거진) 스타일로 제공합니다.

## 제안 사항 및 설계 계획

1. **Routing 및 내비게이션**:
   - `AppRouter.tsx`에 `/community/interviews` 경로 추가.
   - `Header.tsx`에서 '작가님 인터뷰' 링크 연결.

2. **Mock 데이터 구성 (`constants/interviews.ts`)**:
   - `id`, `authorName`(작가명), `workshopName`(소속 공방), `title`(인터뷰 제목/인용구), `summary`, `qaList` (질문/답변 배열 형태의 컨텐츠), `thumbnail`, `profileImage`, `date`.

3. **인터뷰 리스트 페이지 (`Interviews.tsx`) 구현**:
   - 감성적인 인물 중심의 카드형 리스트 레이아웃.
   - 마우스 호버 시 인터뷰 핵심 코멘트(Quote)가 나타나도록 인터랙티브하게 구성.

4. **상세 보기 (Modal)**:
   - 인터뷰를 클릭했을 때 나타나는 에디토리얼 레이아웃의 깊이 있는 모달.
   - Q&A 형식으로 질문과 답변을 깔끔하게 타이포그래피로 분리.
   - 모달 하단에 작가의 공방 보러 가기 링크 제공.

## 검증 계획

### 수동 검증
- `/community/interviews` 경로로 이동 및 컴포넌트 렌더링 확인.
- 카드가 매거진 레이아웃에 맞게 가독성 좋게 출력되는지 확인.
- 상세 보기 클릭 시 Q&A 형식의 인터뷰 내용 모달이 정상적으로 뜨는지 확인.

---

# Plan 25.

# 예약/결제 > 쿠폰/포인트 페이지 구현

사용자가 자신이 보유한 공방 결제용 쿠폰과 CraftDay 적립금(포인트) 내역을 직관적으로 확인하고 관리할 수 있는 대시보드 화면을 구축합니다.

## 제안 사항 및 설계 계획

1. **Routing 및 내비게이션**:
   - `AppRouter.tsx`에 `/my/coupons` 경로 추가.
   - `Header.tsx`의 '예약/결제 > 쿠폰/포인트' 링크 연결 구현.

2. **Mock 데이터 구성 (`constants/rewards.ts`)**:
   - `MyPoints` 객체: 총 보유 포인트, 이번 달 만료 예정 포인트.
   - `PointHistoryItem` 배열: 적립/사용 날짜, 항목명, 금액(+/-), 잔여 포인트.
   - `CouponItem` 배열: 쿠폰명, 할인 금액/비율, 사용 조건(최소 결제액 등), 우대 기한, 상태(사용 가능, 만료 등).

3. **쿠폰/포인트 메인 페이지 (`Coupons.tsx`) 구현**:
   - **Top Dashboard**: 현재 보유한 총 포인트와 보유 쿠폰 개수를 한눈에 볼 수 있는 깔끔한 박스형 요약 UI.
   - **Tabs (쿠폰함 / 포인트 내역)**:
     - 탭 1 (쿠폰함): 쿠폰이 티켓 형태처럼 예쁘게 보이도록 디자인하여, 할인 혜택을 명확히 전달.
     - 탭 2 (포인트 적립/사용 내역): 타임라인이나 깔끔한 표(Table) 형식으로 내역 제공.

## 검증 계획

### 수동 검증
- `/my/coupons` 접속 시 포인트 잔액 요약 패널이 돋보이게 렌더링되는지 확인.
- 쿠폰함 탭과 포인트 내역 탭 전환이 깜빡임 없이 매끄럽게 동작하는지 확인.
- 쿠폰함 탭과 포인트 내역 탭 전환이 깜빡임 없이 매끄럽게 동작하는지 확인.
- 포인트 내역에서 적립(+)과 사용(-)이 직관적인 컬러(예: 녹색/빨간색)로 명확히 구분되는지 확인.

---

# Plan 26.

# 헤더 네비게이션 개편: 예약/결제 메뉴 분리

현재 마우스 호버 시 열리는 대형 드롭다운 메뉴(Mega Menu)에 묶여있던 **'예약/결제'** 하위 항목들을 직관적이고 빠르게 접근할 수 있도록 밖으로 꺼내어 **버튼 형태의 독립된 메뉴**로 재배치합니다.

## 제안 사항 및 설계 계획

1. **기존 드롭다운 항목 제거**:
   - `frontend/src/components/layout/Header.tsx` 내부의 `menus` 배열에서 `예약/결제` 항목 객체를 통째로 삭제합니다.
   
2. **독립 버튼 메뉴 추가**:
   - 헤더의 우측 유저 액션(User Actions: 검색, 장바구니, 로그인 등) 영역 근처 공간을 활용하여 `[예약 내역]`, `[결제 관리]`, `[쿠폰/포인트]` 세 개의 버튼을 직관적으로 배치합니다.
   - 텍스트와 앙증맞은 아이콘(예: Calendar, CreditCard, Ticket 등)을 결합하여 명확한 버튼 형태 UI를 제공합니다.
   - 드롭다운 하위에 있지 않으므로 1-Depth에서 사용자가 언제든 1번의 클릭만으로 마이페이지 핵심 기능에 도달할 수 있습니다.

3. **반응형(모바일/태블릿) 대응 고려**:
   - 메뉴가 많아져 헤더 영역이 좁아지는 것을 대비하여, 데스크톱 환경(lg 사이즈 이상)에서는 온전한 버튼으로 보여주고 태블릿/모바일 크기에서는 아이콘만 노출시키거나 햄버거 메뉴 또는 별도의 User 사이드바 안으로 안전하게 숨기는 방식의 반응형 로직을 검토합니다.

## 검증 계획

### 수동 검증
- 헤더에 배치된 3개의 새 버튼이 미관을 해치지 않고 기존 테마와 잘 어울리는지 색상/크기 균형 확인.
- 브라우저 창 크기를 줄였을 때(반응형), 버튼 메뉴들이 우측 유저 프로필이나 로고와 겹치지 않고 자연스럽게 사라지거나 위치가 변경되는지 확인.
- 각 버튼 클릭 시 `/my/reservations`, `/my/coupons` 등의 페이지로 즉시 이동하는지 확인.

---

# Plan 27.

# 예약/결제 > 결제 관리 페이지 구현

공방 예약 및 굿즈 구매 등을 위해 사용된 결제 내역(영수증)과 간편 결제용 등록 카드 수단을 한 화면에서 관리할 수 있는 **[결제 관리]** 대시보드를 구축합니다.

## 제안 사항 및 설계 계획

1. **Routing 및 내비게이션 연결**:
   - `AppRouter.tsx`에 `/my/payments` 경로를 추가.
   - `Header.tsx`의 예약(달력 아이콘) 드롭다운 메뉴 내 '결제 관리' 링크 경로를 `/my/payments`로 일치.

2. **Mock 데이터 구성 (`constants/payments.ts`)**:
   - `PaymentMethodItem`: 등록 카드 이름, 끝자리 번호 4자리, 대표 카드 여부, 브랜드(Visa, Master 등).
   - `PaymentHistoryItem`: 거래 일시, 공방/상품명, 결제 금액, 결제 상태(결제 완료, 환불 승인, 부분 환불 등), 영수증 번호.

3. **결제 관리 메인 뷰 (`Payments.tsx`) 구현**:
   - **상단 섹션 (내 결제 수단)**: 사용자가 등록해 둔 신용/체크카드들이 실제 실물 카드 형태의 모던한 UI(가로 스크롤 카드 형태)로 예쁘게 나타나도록 구현.
   - **하단 섹션 (결제/환불 내역)**: 거래 발생 날짜, 상호명, 가격, 상태를 타임라인 또는 표 형식으로 구성. 상태(환불 등)에 따라 색상(Red/Green/Slate) 뱃지로 시인성 극대화.

## 검증 계획

### 수동 검증
- 새로 신설된 `/my/payments` 접속 시 오류 없이 렌더링 확인.
- 카드 UI가 실제 신용카드처럼 고급스러운 그라데이션 및 디자인을 가지는지 가시성 체크.
- 결제/환불 거래 내역에서 상태 뱃지(결제완료/환불완료)의 색상 디자인 이질감 확인.

---

# Plan 28.

# 결제 수단 상태 관리 및 CRUD (추가/수정/삭제) 로직 구현

시각적으로 완성된 결제 관리 카드의 액션 버튼(별점, 연필, 쓰레기통, 새 카드 추가 버튼)에 생명력을 불어넣어, 실제 프론트엔드 상태(State) 기반의 동작이 가능하도록 구현합니다. 

## 제안 사항 및 설계 계획

1. **로컬 상태 관리 (Local State)**:
   - `Payments.tsx` 내부에서 정적 Mock 데이터를 초기값으로 삼아 `const [cards, setCards] = useState(MOCK_PAYMENT_METHODS);`로 승급.
   - 배열 조작을 통해 카드의 변경 사항이 실시간으로 화면(UI)에 반영되도록 합니다.

2. **단순 액션 구현 (별표 / 삭제)**:
   - **별표(기본 설정)**: 선택된 카드의 `isDefault`를 `true`로, 다른 모든 카드는 `false`로 일괄 업데이트.
   - **삭제(Trash)**: "정말 이 카드를 삭제하시겠습니까?"라는 브라우저 기본 컨펌(`window.confirm`) 또는 Custom 모달로 재문의한 뒤, 배열에서 필터링(`filter`)하여 제외.

3. **복합 액션 구현 (추가 / 수정)**:
   - **신규 추가(+) 및 수정(Pencil)** 버튼을 누르면 팝업되는 **CardFormModal** 구성.
   - 입력 필드: 카드 별칭(이름), 카드 번호(16자리), 브랜드(분류), 테마 컬러(Violet, Rose 등 선택 가능).
   - 카드 번호 입력 시 자동으로 가운데 자리를 `****` 로 마스킹 처리하여 저장(`cardNumberMasked`).
   - 저장 시 `setCards(prev => [...prev, newCard])` 혹은 기존 데이터 매핑(`map`) 교체.

## 검증 계획

### 수동 검증
- 대표 카드(별표) 아이콘 누를 시, 정상적으로 뱃지(기본 결제)가 이동하는지 렌더링 검사.
- 삭제(Trash) 아이콘 클릭 후 컨펌 승인 시 화면에서 즉각 카드가 소멸되는지 확인.
- 새 결제 수단 추가 모달 폼에서 올바르게 타이핑이 되고 Submit하면 새 카드 위젯이 목록 최우측에 부화하는지 확인.

---

# Plan 29.

# 결제 및 환불 내역 상세 보기 구현

결제 내역 목록에서 특정 항목을 클릭했을 때, 해당 거래의 상세 정보(상품명, 금액, 주문번호, 결제수단 등)를 영수증 스타일의 모달로 보여주는 기능을 구현합니다.

## 제안 사항 및 설계 계획

1. **인터렉티브 리스트**:
   - `Payments.tsx`의 결제 내역 테이블 로우에 클릭 이벤트 및 호버 스타일 적용.
   - 클릭 시 선택된 항목 데이터를 상태로 관리 (`selectedHistory`).

2. **영수증 스타일 상세 모달 (`HistoryDetailModal`)**:
   - 상단: '결제 상세 정보' 제목과 Receipt 아이콘 배치.
   - 중앙: 상품명, 결제 완료/환불 상태 뱃지, 주문 번호, 결제 수단 상세, 거래 일시 노출.
   - 하단: 최종 결제 금액 강조 및 영수증 저장/닫기 버튼 배치.
   - 기존의 요소 겹침(Negative margin)을 제거하고 수직으로 정렬하여 레이아웃 안정성 확보.

3. **UI/UX 최적화**:
   - 모달 내부 패딩 및 간격 조정 (`p-6` 등으로 컴팩트하게 구성).
   - `animate-in zoom-in` 효과로 부드러운 전환 제공.

## 검증 계획

### 수동 검증
- 결제 내역 로우 클릭 시 정확한 데이터가 담긴 상세 모달이 열리는지 확인.
- 모달 내의 '닫기' 버튼 및 외부 영역 클릭 시 정상적으로 닫히는지 확인.
- 모바일 및 다크 모드 환경에서 영수증 UI가 깨지지 않고 가독성이 유지되는지 확인.

---

# Plan 30.

# 이용약관 및 개인정보처리방침 페이지 구현

사용자가 서비스 이용 규칙과 개인정보 보호 방침을 명확히 확인할 수 있도록 텍스트 기반의 전용 페이지를 구축하고, 푸터(Footer)를 통해 접근할 수 있게 합니다.

## 제안 사항 및 설계 계획

1. **Routing 설정**:
   - `frontend/src/routes/AppRouter.tsx`: `/terms` (이용약관), `/privacy` (개인정보처리방침) 경로 추가.
   
2. **페이지 컴포넌트 구현**:
   - `frontend/src/pages/legal/Terms.tsx` [NEW]: 이용약관 텍스트 컨텐츠를 담은 페이지.
   - `frontend/src/pages/legal/Privacy.tsx` [NEW]: 개인정보처리방침 텍스트 컨텐츠를 담은 페이지.
   - 가독성을 위해 적절한 레이아웃(최대 너비 제한, 여백)과 타이포그래피 적용.

3. **푸터 연동**:
   - `frontend/src/components/layout/Footer.tsx`: 기존의 단순 텍스트(`span`)를 `Link` 컴포넌트나 `navigate`를 활용한 이동 링크로 변환.

## 검증 계획

### 수동 검증
- 푸터 하단의 '이용약관' 및 '개인정보처리방침' 클릭 시 각각 올바른 페이지로 이동하는지 확인.
- 텍스트 위주의 페이지에서 모바일 가동성(가독성) 및 레이아웃이 정상인지 확인.
- 다크 모드 적용 시 텍스트 컬러가 배경색과 잘 조화되는지 확인.

---

# Plan 31.

# 백엔드 API 응답 구조 변경 대응 (BaseResponse 도입)

백엔드 API의 응답 구조가 `BaseResponse<T>` (message, data 포함) 형식으로 변경됨에 따라, 프론트엔드의 공통 타입과 실제 API 호출부(`AccessLog.tsx`)를 수정하여 연동을 정상화합니다.

## 제안 사항 및 설계 계획

1. **공통 타입 정의**:
   - `frontend/src/types/common.ts`: 백엔드의 `BaseResponse<T>`와 대응하는 인터페이스 추가.
     ```typescript
     export interface BaseResponse<T> {
         message: string;
         data: T;
     }
     ```

2. **API 호출 로직 수정**:
   - `frontend/src/pages/admin/access/index.tsx`:
     - `api.get`의 제네릭 타입을 `BaseResponse<PageResponseDto<UserAccessLogList>>`로 변경.
     - `response.data.data`를 통해 실제 데이터(`PageResponseDto`)에 접근하도록 수정.

3. **영향 범위 확인**:
   - 현재 실데이터를 연동 중인 다른 관리자 페이지 및 서비스가 있는지 전수 조사하고, 필요 시 동일한 구조로 수정 적용.

## 검증 계획

### 수동 검증
- 접속 이력 관리 페이지 진입 시 데이터가 정상적으로 로드되는지 확인.
- 브라우저 개발자 도구의 Network 탭을 통해 응답 데이터가 `BaseResponse` 구조인지, 프론트엔드에서 이를 정확히 파싱하는지 확인.

---

# Plan 32.

# 네비게이션 메뉴 경로 연결 (공방 탐색)

헤더(Header)의 드롭다운 메뉴 중 '공방 탐색 > 카테고리별 클래스' 항목이 실제 공방 리스트 페이지(`/workshops`)로 연결되도록 수정하여 사용자 내비게이션 환경을 개선합니다.

## 제안 사항 및 설계 계획

1. **메뉴 경로 수정**:
   - `frontend/src/components/layout/Header.tsx`: `menus` 배열 내의 '카테고리별 클래스' 객체의 `path` 값을 `#`에서 `/workshops`로 변경합니다.

2. **추가 점검**:
   - 기타 미연결 메뉴(실시간 핫플레이스, 신규 공방 등)의 경우 현재 구현된 페이지가 없으므로 그대로 두거나 필요 시 메인으로 유도합니다.

## 검증 계획

### 수동 검증
- 헤더의 '공방 탐색' 메뉴에 마우스를 올려 드롭다운을 열고, '카테고리별 클래스'를 클릭했을 때 `/workshops` 페이지로 정상 이동하는지 확인합니다.
- 이동 후 공방 리스트가 정상적으로 렌더링되는지 확인합니다.
